# gympass

Instalar Node.js y Android Studio (emulador de android) o usar expo app

npx create-expo-app gympass --template blank
npx expo start

Instalar firebase para el manejo de bdd y backend

npm install firebase